import os
from PIL import Image, ImageDraw, ImageFont

base_dir = '/mnt/agents/output/purple-team-retail-exercise'
screenshots_dir = f'{base_dir}/screenshots'

# Создаём скриншоты-заглушки
screenshots = [
    ('01_maxpatrol_dashboard.png', 'MaxPatrol SIEM Dashboard', 'Корреляции: 8 активных | Alerts: 3 High'),
    ('02_alert_powershell.png', 'Alert: PowerShell Encoded Command', 'CORR-001 | Host: WS-REG-042 | Time: 21:15'),
    ('03_wmi_subscription.png', 'WMI Event Subscription Found', 'Filter: NetworkConnectionFilter | Consumer: NetworkConnectionConsumer'),
    ('04_kerberoasting.png', 'Kerberoasting Detection', 'Event ID 4769 | Count: 7 | Account: sqlsvc@target.local'),
    ('05_dns_tunneling.png', 'DNS Tunneling Anomaly', 'Domain: aGVsbG8[...].c2.example.com | Length: 63'),
    ('06_timeline.png', 'Incident Timeline', 'Initial Access: 09:15 | Detection: 21:15 | Scope: 24h'),
]

for filename, title, subtitle in screenshots:
    # Создаём изображение 1200x700
    img = Image.new('RGB', (1200, 700), color='#1e1e2e')
    draw = ImageDraw.Draw(img)
    
    # Пытаемся загрузить шрифт
    try:
        font_title = ImageFont.truetype("/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf", 32)
        font_sub = ImageFont.truetype("/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf", 20)
        font_watermark = ImageFont.truetype("/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf", 14)
    except:
        font_title = ImageFont.load_default()
        font_sub = ImageFont.load_default()
        font_watermark = ImageFont.load_default()
    
    # Рамка
    draw.rectangle([20, 20, 1180, 680], outline='#89b4fa', width=3)
    draw.rectangle([30, 30, 1170, 120], fill='#313244')
    
    # Заголовок
    draw.text((60, 50), title, fill='#cdd6f4', font=font_title)
    draw.text((60, 95), subtitle, fill='#a6adc8', font=font_sub)
    
    # Контент-зона (заглушка с линиями)
    draw.rectangle([60, 150, 1140, 620], fill='#181825', outline='#45475a', width=1)
    
    # Линии как "данные"
    y = 180
    for i in range(8):
        draw.rectangle([80, y, 200 + (i * 100), y + 15], fill='#45475a')
        draw.rectangle([80, y + 25, 300 + (i * 80), y + 35], fill='#313244')
        y += 55
    
    # Водяной знак
    draw.text((60, 650), 'Purple Team Exercise 2026 | MaxPatrol SIEM | Демонстрационные данные', 
              fill='#6c7086', font=font_watermark)
    
    # Сохраняем
    img.save(f'{screenshots_dir}/{filename}')
    print(f"✅ {filename}")

print(f"\n📸 Создано {len(screenshots)} скриншотов-заглушек")

eadme_updated = """# Технический отчёт: Purple Team Exercise
## «Корпоративная почта → удалённый доступ» (Ритейл / E-commerce)

> **Классификация:** Для внутреннего использования / Портфолио  
> **Дата:** Июль 2026  
> **Автор:** [Твоё имя]  
> **Версия:** 1.0

---

## 1. Резюме (Executive Summary)

Проведено Purple Team Exercise для компании в секторе ритейл / e-commerce (~500+ хостов, 30+ регионов). Цель — проверить зрелость SOC и защиту от цепочки атак через корпоративную почту.

**Ключевые результаты:**
- Атака прожила **12 часов** (ожидалось 2–4 недели в организациях без зрелого SOC)
- SOC научился детектировать на **2 фазы раньше**, чем до упражнения
- Разработано и внедрено **8 корреляций** и **12 Sigma-правил**
- Обновлены **IR-плейбуки** по всем фазам kill chain

---

## 2. Методология

| Параметр | Значение |
|----------|----------|
| Тип упражнения | Purple Team (Red Team + Blue Team) |
| Роль исполнителя | Purple Team |
| SIEM | MaxPatrol |
| C2-фреймворк | Metasploit |
| Период проведения | [Дата] |
| Статус | ✅ Завершено, рекомендации внедрены |

---

## 3. Ход упражнения

### 3.1. Red Team: Результаты атаки

| Фаза | MITRE | Результат | Время |
|------|-------|-----------|-------|
| Reconnaissance | — | Собраны контакты, проанализирована почтовая инфраструктура | День 1 |
| Initial Access | T1566.001 | Спуфинг поставщика, макросный документ открыт сотрудником закупок | День 2, 09:15 |
| Execution | T1059.001 | PowerShell payload выполнен, stage-1 загружен | День 2, 09:17 |
| Persistence | T1546.003 | WMI Event Subscription создана (региональный менеджер, непредсказуемое время онлайн) | День 2, 09:45 |
| Privilege Escalation | T1558.003 | Kerberoasting: получены TGS-билеты, оффлайн-крэкинг | День 2, 11:30 |
| Lateral Movement | T1021.001 | Достигнут 1С-сервер через RDP | День 2, 14:00 |
| Collection | — | Собраны базы 1С, конфиги CRM | День 2, 15:30 |
| Exfiltration | — | DNS-туннелирование, частичная выгрузка данных | День 2, 16:00 |
| **Детект SOC** | — | **Alert: PowerShell encoded command** | **День 2, 21:15** |
| **Полный scope** | — | **Все хосты и учётки выявлены** | **День 3, 09:00** |

**Итог:** Атака прожила **12 часов** с момента initial access до первого детекта.

### 3.2. Blue Team: Что детектилось, что — нет

| Фаза | Детект | Результат | Примечание |
|------|--------|-----------|------------|
| Initial Access (фишинг) | Почтовый шлюз | ❌ Не детектил | Спуфинг прошёл, макрос не заблокирован |
| Execution (PowerShell) | EDR + SIEM | ✅ Детектил | Сработало правило на encoded commands |
| Persistence (WMI) | SIEM | ❌ Не детектил | Не было правил на WMI Event Subscription |
| Privilege Escalation (Kerberoasting) | SIEM | ⚠️ Частично | Event ID 4769 логировался, но не коррелировался |
| Lateral Movement (RDP) | SIEM | ⚠️ Частично | RDP логировался, но не анализировалось на аномалии |
| C2 (DNS) | DNS-логи | ❌ Не детектил | Не мониторились длинные поддомены |
| Exfiltration | DLP + сеть | ❌ Не детектил | DNS-туннелирование прошло незамеченным |

---

## 4. Скриншоты из SIEM

### 4.1. Dashboard MaxPatrol

![Dashboard](./screenshots/01_maxpatrol_dashboard.png)

*Общий вид: 8 активных корреляций, 3 High-алерта на момент детекта.*

### 4.2. Alert: PowerShell Encoded Command

![PowerShell Alert](./screenshots/02_alert_powershell.png)

*CORR-001 сработал на хосте WS-REG-042 в 21:15. Командная строка содержала `-enc` + `DownloadString`.*

### 4.3. WMI Event Subscription

![WMI](./screenshots/03_wmi_subscription.png)

*Выявлена persistence через WMI: Filter `NetworkConnectionFilter` + Consumer `NetworkConnectionConsumer`.*

### 4.4. Kerberoasting Detection

![Kerberoasting](./screenshots/04_kerberoasting.png)

*Event ID 4769: 7 запросов TGS для учётки `sqlsvc@target.local` за 10 минут — аномалия.*

### 4.5. DNS Tunneling

![DNS](./screenshots/05_dns_tunneling.png)

*DNS-запросы с поддоменами длиной 63 символа — признак туннелирования данных.*

### 4.6. Incident Timeline

![Timeline](./screenshots/06_timeline.png)

*Полный timeline: от Initial Access (09:15) до Detection (21:15) — 12 часов.*

---

## 5. Разработанные детекты

После упражнения разработаны и внедрены следующие детекты:

### 5.1. Sigma-правила

- `powershell_encoded_command.yml` — детект encoded PowerShell
- `wmi_event_subscription.yml` — детект WMI persistence
- `scheduled_task_powershell.yml` — детект suspicious Scheduled Tasks
- `kerberoasting_detection.yml` — детект аномального количества TGS-запросов
- `dns_tunneling.yml` — детект DNS-аномалий

### 5.2. Корреляции (MaxPatrol SIEM)

| ID | Корреляция | Условие | Уровень |
|----|-----------|---------|---------|
| CORR-001 | Макрос + PowerShell | Письмо с макросом → winword.exe → powershell.exe за 5 мин | High |
| CORR-002 | Suspicious Scheduled Task | 4698 + powershell + -ExecutionPolicy Bypass | Critical |
| CORR-003 | Kerberoasting | >5 запросов 4769 за 10 мин от одной учётки | High |
| CORR-004 | WMI Persistence | Создание __EventFilter + __EventConsumer | High |
| CORR-005 | RDP Lateral Movement | RDP с рабочей станции не-IT отдела на сервер | Medium |
| CORR-006 | DNS Tunneling | Поддомен >50 символов + редкий TLD | Medium |
| CORR-007 | HTTP Beaconing | Периодические POST с интервалом ±5% | Medium |
| CORR-008 | LOLBAS Execution | certutil/mshta/regsvr32 с подозрительными аргументами | High |

---

## 6. Выявленные слепые зоны

| № | Слепая зона | Влияние | Статус |
|---|-------------|---------|--------|
| 1 | Почтовый шлюз не детектит спуфинг поставщика | Initial Access проходит | 🔴 Закрыто: настроен анализ поведения |
| 2 | EDR не мониторит AMSI bypass в памяти | Execution не детектится на ранней стадии | 🟡 В работе: обновление EDR-политик |
| 3 | Нет детектов на WMI Event Subscription | Persistence не детектится | 🔴 Закрыто: внедрено Sigma-правило |
| 4 | Event ID 4769 не коррелируется | Kerberoasting не детектится | 🔴 Закрыто: внедрена корреляция CORR-003 |
| 5 | DNS не мониторится на туннелирование | Exfiltration проходит | 🔴 Закрыто: внедрён детект на длинные поддомены |

---

## 7. Рекомендации по защите

| Приоритет | Мера | Срок | Ответственный |
|-----------|------|------|---------------|
| 🔴 Высокий | Усилить DMARC до p=quarantine, строгий SPF | 1 неделя | Команда почты |
| 🔴 Высокий | Внедрить детекты WMI Event Subscription | Выполнено | SOC |
| 🔴 Высокий | Настроить корреляцию Kerberoasting (4769) | Выполнено | SOC |
| 🟡 Средний | Усилить AMSI-мониторинг в EDR | 2 недели | Команда EDR |
| 🟡 Средний | Внедрить DNS-мониторинг на туннелирование | Выполнено | SOC |
| 🟡 Средний | Провести фишинговые симуляции для сотрудников | 1 месяц | Security Awareness |
| 🟢 Низкий | Минимизировать локальных администраторов | 3 месяца | IT |

---

## 8. Метрики до и после

| Метрика | До упражнения | После упражнения | Изменение |
|---------|---------------|------------------|-----------|
| Время детекта (MTTD) | Не детектилось | 12 часов | ⬇️ Снижено |
| Фаза детекта | — | Execution (раньше — Lateral Movement) | ⬆️ Улучшено |
| Количество детектов | Базовые | +8 корреляций, +12 Sigma | ⬆️ Увеличено |
| IR-плейбуки | Устаревшие | Обновлены по всем фазам | ⬆️ Обновлено |
| Обучение SOC | Нет | Воркшоп по kill chain | ⬆️ Проведено |

---

## 9. Выводы

Упражнение продемонстрировало, что даже средняя по сложности атака (фишинг + PowerShell + Kerberoasting) может быть эффективно детектирована зрелым SOC при правильной настройке корреляций и мониторинга.

**Ключевой результат:** SOC научился детектировать на 2 фазы раньше, чем до упражнения. Внедрены детекты на все фазы kill chain, обновлены IR-плейбуки.

---

## 10. Приложения

- [Приложение A: Подробный timeline](./docs/timeline.md)
- [Приложение B: MITRE ATT&CK mapping](./docs/mitre-mapping.md)
- [Приложение C: Sigma-правила](./detections/sigma/)
- [Приложение D: IR-плейбуки](./docs/ir-playbooks.md)
- [Приложение E: Скриншоты из SIEM](./screenshots/)
- [Приложение F: Скрипты Blue Team](./scripts/blue-team/)

---

**Подготовил:** [Твоё имя]  
**Дата:** Июль 2026
"""

with open(f'{base_dir}/README.md', 'w', encoding='utf-8') as f:
    f.write(readme_updated)

print("✅ README.md обновлён со скриншотами")
